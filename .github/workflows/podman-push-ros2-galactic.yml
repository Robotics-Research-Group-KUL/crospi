name: Podman test galactic - Build & Push testing Images for ROS2 galactic

on:
  push:
    branches: [ main ]
    paths:
      - .podman/user_containers/galactic/Containerfile

  release:
    types: [published]

permissions:
  contents: read
  packages: write

jobs:
  build-base:
    name: Build & Push Base Image (Podman)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Podman
        run: |
          sudo apt-get update
          sudo apt-get -y install podman

      - name: Login to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build image
        run: |
          podman build \
            -f .podman/user_containers/galactic/Containerfile \
            -t ghcr.io/robotics-research-group-kul/crospi:podman-ros2-galactic \
            -t ghcr.io/robotics-research-group-kul/crospi:podman-ros2-galactic-${{ github.sha }} \
            .

      - name: Push image
        run: |
          podman push ghcr.io/robotics-research-group-kul/crospi:podman-ros2-galactic
          podman push ghcr.io/robotics-research-group-kul/crospi:podman-ros2-galactic-${{ github.sha }}